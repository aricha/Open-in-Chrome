//
// Prefix header for all source files of the 'openInChrome' target in the 'openInChrome' project
//

#ifdef __OBJC__
    #import <Foundation/Foundation.h>

#ifdef DEBUG
#define DLog(format, ...) NSLog(format)
#else
#define DLog(format, ...)
#endif

#endif
